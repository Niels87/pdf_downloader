use std::{collections::HashMap, fs::File, path::PathBuf};

//use tokio::fs::*;
use csv::*;
use color_eyre::{eyre::Ok, *};

// pub fn write_log(writer: &mut Writer<File>, log: (String, bool)) -> eyre::Result<()> {

    

//     Ok(())
// }

